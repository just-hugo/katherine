{
  "dependencies": {
    "animate.css": "^4.1.1",
    "bootstrap": "^5.3.2",
    "live-server": "^1.1.0",
    "sass": "^1.69.5"
  },
  "scripts": {
    "devserver": "live-server --browser='Google Chrome' --no-css-inject --wait=100"
  }
}
